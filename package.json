{
  "name": "web-dashboard-virtual-assistant",
  "version": "1.0.0",
  "description": "A comprehensive web dashboard virtual assistant with React frontend, Express backend, MongoDB, and OpenAI integration",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm run build",
    "start": "cd server && npm start"
  },
  "keywords": ["dashboard", "virtual-assistant", "react", "express", "mongodb", "openai"],
  "author": "DprinceDeveloper",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}